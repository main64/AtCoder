#include <iostream>
#include <vector>
#include <cmath>

using namespace std;


int main(){
    int T,N;
    cin>>T;
    for(int t=0;t<T;t++){
        cin>>N;
        long Npow=pow(2,N);
        string s;
        cin>>s;
        int m=1;
        while(m<=N && )
    }
}